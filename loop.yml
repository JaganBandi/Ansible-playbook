---
- name: Create multiple project Directories
  hosts: all
  become: yes

  tasks:
    - name: Create project directories
      file:
        path: "/opt/{{item}}"
        state: directory
        mode: '0755'


      loop:
        - dev
        - tet
        - prop
        - backup
      
